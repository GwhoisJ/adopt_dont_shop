<% @shelters.each_with_index do |shelter, index| %>
  <div id="shelter-<%=index%>">
    <p><%=shelter.name%></p>
  </div>
<% end %>

<h3>Shelters with Pending Applications</h3>

<div id="pending">
  <% @pendings.each do |shelter| %>
    <p><%= shelter.name %></p>
  <% end %>
</div>